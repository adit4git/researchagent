# Core - all free
httpx>=0.27.0
ddgs>=6.0.0          # DuckDuckGo search, no API key needed
readability-lxml>=0.8.1   # Strip boilerplate from web pages
lxml_html_clean>=0.1.0    # Required by readability-lxml
beautifulsoup4>=4.12.0
tiktoken>=0.7.0           # Token counting (works for any model approximately)
pydantic>=2.0.0
rich>=13.7.0              # Pretty CLI output
streamlit>=1.35.0         # Web UI

# LLM providers - install only what you need
groq>=0.11.0              # Free tier, fast
google-genai>=1.0.0       # Gemini, free tier (replaces deprecated google-generativeai)
ollama>=0.3.0             # Local, fully free
anthropic>=0.39.0         # Optional paid
openai>=1.50.0            # Optional paid
